import "cesium/Build/Cesium/Widgets/widgets.css";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Night Editor",
};

export default function NightEditorLayout({ children }: { children: React.ReactNode }) {
  return children;
}
